{
  "name": "micclaude",
  "version": "0.1.0",
  "private": true,
  "description": "Front end for micclaude: capture, wake-word detection and streaming replies",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/js/*.test.js\"",
    "test:e2e": "node --test \"tests/e2e/*.test.js\""
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
